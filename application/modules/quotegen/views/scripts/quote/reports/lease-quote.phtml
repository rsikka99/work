<?php $quote = $this->Quote(); /* @var $quote Quotegen_Model_Quote */ ?>
<div class="row">
    <div class="span12">
        <?php foreach ($quote->getQuoteDeviceGroups() as $quoteDeviceGroup) : /* @var $quoteDeviceGroup Quotegen_Model_QuoteDeviceGroup */ ?>
        <?php if($quoteDeviceGroup->calculateTotalQuantity() > 0 ) : ?>
        	<h3><?php echo $this->escape($quoteDeviceGroup->getName()); ?></h3>
            <table class="table table-condensed table-bordered table-striped quoteDeviceTable">
                <tr class="header-row">
                    <th style="width: 220px;">Item</th>
                    <th style="width: 140px;">Sku</th>
                    <th>Description</th>
                    <th style="width: 80px;">Price</th>
                    <th style="width: 65px;">Quantity</th>
                    <th style="width: 70px;">Subtotal</th>
                </tr>
                <?php foreach ($quoteDeviceGroup->getQuoteDeviceGroupDevices() as $quoteDeviceGroupDevice) : /* @var $quoteDeviceGroupDevice Quotegen_Model_QuoteDeviceGroupDevice */ ?>
                    <?php if($quoteDeviceGroupDevice->getQuantity() > 0) : ?>
                    	<tr class="deviceRow">
                	        <th><h5><?php echo $quoteDeviceGroupDevice->getQuoteDevice()->name; ?></h5></th>
            	        <td><?php echo $quoteDeviceGroupDevice->getQuoteDevice()->oemSku; ?></td>
        	            <td></td>           
    	                <td style="text-align: right;"></td>
	                    <td style="text-align: right;"><?php echo number_format($quoteDeviceGroupDevice->getQuantity()); ?></td>
                	    <td style="text-align: right;"></td>
            	        </tr>
        	            <?php if (count($quoteDeviceGroupDevice->getQuoteDevice()->getQuoteDeviceOptions()) > 0) : ?>
    	                    <?php foreach ($quoteDeviceGroupDevice->getQuoteDevice()->getQuoteDeviceOptions() as $quoteDeviceOption) : /* @var $quoteDeviceOption Quotegen_Model_QuoteDeviceOption */ ?>
	                            <tr class="optionRow">
                        	        <td><?php echo $quoteDeviceOption->getName(); ?></td>
                    	            <td><?php echo $quoteDeviceOption->getOemSku(); ?></td>
                	                <td><?php echo $quoteDeviceOption->getDescription(); ?></td>
            	                    <td></td>
        	                        <td></td>
    	                            <td></td>
	                            </tr>
                        	<?php endforeach; ?>
                    	<?php endif; ?>
                    <?php endif; ?>
                <?php endforeach; ?>
                <?php if($quoteDeviceGroup->calculateTotalMonochromePages() > 0) : ?>
                <tr class="pagesRow">
                    <td>Monochrome Pages</td>
                    <td></td>
                    <td>Pages Included</td>
                    <td></td>
                    <td style="text-align: right;"><?php echo number_format($quoteDeviceGroup->calculateTotalMonochromePages()); ?> </td>
                    <td></td>
                </tr>
                <?php endif ?>
                <?php if($quoteDeviceGroup->calculateTotalColorPages() > 0) : ?>
                <tr class="pagesRow">
                    <td>Color Pages</td>
                    <td></td>
                    <td>Pages Included</td>
                    <td></td>
                    <td style="text-align: right;"><?php echo number_format($quoteDeviceGroup->calculateTotalColorPages()); ?> </td>
                    <td></td>
                </tr>
                <?php endif ?>
                <tr>
                    <td colspan="5"></td>
                    <th style="text-align: right;"><?php echo $this->currency($quoteDeviceGroup->calculateMonthlyLeasePrice()); ?></th>
                </tr>
        	</table>
			<?php endif; ?>
    	<?php endforeach; ?>
        <table class="table table-condensed table-bordered table-striped quoteDeviceTable">
            <tr class="pagesRow">
                <td style="text-align: right;" colspan="5">Monochrome overage price</td>
                <td style="text-align: right; width: 70px;"><?php echo $this->currency($quote->calculateMonochromeOverageRatePerPage(),array('precision' => 3)); ?></td>
            </tr>
            <tr class="pagesRow">
                <td style="text-align: right;" colspan="5">Color overage price</td>
                <td style="text-align: right; width: 70px"><?php echo $this->currency($quote->calculateColorOverageRatePerPage(), array('precision' => 3)); ?></td>
            </tr>
        </table>
        <div class='pull-right'>
            <h3>Quote Monthly Total: <?php echo $this->currency($quote->calculateQuoteMonthlyLeaseSubtotal()); ?></h3>
        </div>
    </div>
</div>
