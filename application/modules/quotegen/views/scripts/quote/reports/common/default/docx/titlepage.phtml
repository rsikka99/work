<?php
/**
 * Assessment Title Page
 */
/* @var $phpword PHPWord */
$phpword = $this->phpword;

/* @var $quote Quotegen_Model_Quote */
$quote = $this->quote;

$client = $quote->getClient();


$quoteDate = date('F jS, Y', strtotime($quote->getQuoteDate()));

/* @var $section PHPWord_Section */
$section = $this->section;

$clientName = $quote->getClientDisplayName();
if (empty($clientName))
{
    $clientName = $client->getCompanyName();
}

$section->addText($this->translate('Prepared For:') . " " . $clientName, 'titlepageHeaderFont', 'titlepageHeaderParagraph');

$section->addText($quoteDate, 'titlepageHeaderFont', 'titlepageHeaderParagraph');
$section->addTextBreak(4);

//add the addresses
$section->addText($client->getAddress()->getAddressLine1(), null, 'nospacing');

if (strlen($client->getAddress()->getAddressLine2()) > 0)
{
	$section->addText($client->getAddress()->getAddressLine2(), null, 'nospacing');
}

$section->addText("{$client->getAddress()->getCity()}, {$client->getAddress()->getRegion()} {$client->getAddress()->getPostCode()}", null, 'nospacing');
$section->addText($client->getAddress()->getCountry(), null, 'nospacing');
