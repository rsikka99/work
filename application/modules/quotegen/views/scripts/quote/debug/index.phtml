<?php 
$quote = $this->Quote(); /* @var $quote Quotegen_Model_Quote */
$this->placeholder('ProgressionNav')->set($this->render('_partials/quoteHeading.phtml'));

$this->headScript()->appendScript("$('i').popover();");

?>
<div class="well">
<h1>Numbers if purchasing</h1>
<pre>The quote value when purchasing is <?php echo $this->currency($quote->calculateQuoteSubtotal()); ?>.</pre>
<h1>Numbers if leasing</h1>
<pre>When leasing this quote will be leased for <?php echo $quote->getLeaseTerm(); ?> Months at a factor of <?php echo $quote->getLeaseRate(); ?>.
The quote value is <?php echo $this->currency($quote->calculateQuoteLeaseValue()); ?>.
The quote monthly payment is <?php echo $this->currency($quote->calculateTotalMonthlyLeasePrice()) ; ?> per month.



</pre>
<h1>TODO: Add Pages</h1>
</div>