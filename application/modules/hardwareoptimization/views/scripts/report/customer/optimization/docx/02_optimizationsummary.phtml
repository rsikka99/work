<?php
/* @var $section \PhpOffice\PhpWord\Element\Section */
$section = $this->section;

/* @var $graphs array */
$graphs = $this->graphs;

$section->addPageBreak();

$section->addTitle('Current Fleet Statistics', 2);

$imageTable = $section->addTable($this->styles->tables->doubleimage->tablestyle);
$imageTable->addRow();
$imageTable->addCell(4514, $this->styles->tables->doubleimage->cell1)->addImage($graphs [0], ['align' => 'center']);
$imageTable->addCell(4514, $this->styles->tables->doubleimage->cell2)->addImage($graphs [1], ['align' => 'center']);
$imageTable->addCell(4514, $this->styles->tables->doubleimage->cell3)->addImage($graphs [2], ['align' => 'center']);

$section->addTitle('Optimization Summary', 2);

$section->addText($this->translate('All fleets vary in age and functionality. This report sorts the devices in your fleet into four categories, based on page count, functionality, and optimal device age. For the purposes of this report, the categories are as follows:'));
$section->addText($this->translate('Keep: Devices are being used within their specifications.'));
$section->addText($this->translate('Replace: Refer to the Appendix for reasons for device replacements.'));
$section->addText($this->translate("Do Not Repair: These devices may remain in place but we don't recommend repairs if they are needed. The reasons can be any one of the following: the device is older than 10 years, it has printed more than its recommended maximum lifetime page count, or it is incompatible with an MPS program."));
$section->addText($this->translate('Retire/Migrate: The device prints fewer than 200 pages per month; you may choose to migrate pages from these devices to other nearby devices to save on supply and electrical costs.'));

$section->addImage($graphs[3], $this->styles->images->center);
$section->addTextBreak();