<?php /* @var $masterDevice Proposalgen_Model_MasterDevice */ $masterDevice = $this->masterDevice; ?>
<div class="page-header">
    <h1>Master Device Management</h1>
</div>
<div class="well">
    <dl class="dl-horizontal">
        <dt>Manufacturer</dt><dd><?php echo $masterDevice->getManufacturer()->fullname; ?></dd>
        <dt>Name</dt><dd><?php echo $masterDevice->modelName; ?></dd>
        <dt>Full Name</dt><dd><?php echo $masterDevice->getFullDeviceName(); ?></dd>
        
        <dt>Toner Config</dt><dd><?php echo $masterDevice->getTonerConfig()->tonerConfigName; ?></dd>
        <dt>Is Copier</dt><dd><?php echo ($masterDevice->isCopier) ? "Yes" : "No"; ?></dd>
        <dt>Is Fax</dt><dd><?php echo ($masterDevice->isFax) ? "Yes" : "No"; ?></dd>
        <dt>Is Scanner</dt><dd><?php echo ($masterDevice->isScanner) ? "Yes" : "No"; ?></dd>
        <dt>Is Duplex</dt><dd><?php echo ($masterDevice->isDuplex) ? "Yes" : "No"; ?></dd>
        <dt>Is Replacement Device</dt><dd><?php echo ($masterDevice->isReplacementDevice) ? "Yes" : "No"; ?></dd>
        <dt>Watts Power Normal</dt><dd><?php echo ($masterDevice->wattsPowerNormal) ?: 'Not Set'; ?></dd>
        <dt>Watts Power Idle</dt><dd><?php echo ($masterDevice->wattsPowerIdle) ?: 'Not Set'; ?></dd>
        <dt>Device Price</dt><dd><?php echo ($masterDevice->getDealerAttributes()->cost) ? $this->currency($masterDevice->getDealerAttributes()->cost) : 'No Price Defined'; ?></dd>
        <dt>Parts Cost Per Page</dt><dd><?php echo ($masterDevice->getDealerAttributes()->partsCostPerPage) ? $this->currency($masterDevice->getDealerAttributes()->partsCostPerPage, array('precision' => 4)) : 'No Price Defined'; ?></dd>
        <dt>Labor Cost Per Page</dt><dd><?php echo ($masterDevice->getDealerAttributes()->laborCostPerPage) ? $this->currency($masterDevice->getDealerAttributes()->laborCostPerPage, array('precision' => 4)) : 'No Price Defined'; ?></dd>
        <dt>Launch Date</dt><dd><?php echo ($masterDevice->launchDate) ?: 'Not Set'; ?></dd>
        <dt>Date Created</dt><dd><?php echo ($masterDevice->dateCreated) ?: 'Not Set'; ?></dd>
        <dt>Duty Cycle</dt><dd><?php echo ($masterDevice->dutyCycle) ?: 'Not Set'; ?></dd>
        <dt>PPM Black</dt><dd><?php echo ($masterDevice->ppmBlack) ?: 'Not Set'; ?></dd>
        <dt>PPM Color</dt><dd><?php echo ($masterDevice->ppmColor) ?: 'Not Set'; ?></dd>
        <dt>Is Leased</dt><dd><?php echo ($masterDevice->isLeased) ? "Yes" : "No"; ?></dd>
        <?php if ($masterDevice->isLeased) : ?>
        <dt>Leased Toner Yield</dt><dd><?php echo number_format($masterDevice->leasedTonerYield); ?></dd>
        <?php endif; ?>
    </dl>
</div>